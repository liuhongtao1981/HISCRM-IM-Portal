# 实时监控独立 Tab 解决方案

**日期**: 2025-11-07 14:11
**问题**: 实时监控与登录检测争抢同一个创作者中心 Tab
**解决方案**: 实时监控使用独立 Tab + 登录检测使用默认页
**状态**: ✅ 已完成

---

## 🐛 原问题

### 症状

用户报告：
1. Worker 启动后只有 2 个 Tab：默认页 + 创作者中心
2. **实时监控的钩子被注入到登录检测使用的 Tab 上**
3. 开发者工具显示 `typeof window.__checkRealtimeHooks` 返回 `"function"` ✅
4. 但这个 Tab 被登录检测占用，不是实时监控的专用 Tab

### 根本原因

**Tab 资源冲突**：

```
Worker 启动流程:
  ├── AccountStatusReporter.start()
  │   └── checkLoginStatus()
  │       ├── 获取/创建 Tab (tag: LOGIN_CHECK)
  │       ├── 导航到 creator.douyin.com
  │       └── 检测登录状态
  │
  └── MonitorTask.start()
      └── startRealtimeMonitor()
          ├── 查找已存在的创作者中心 Tab ❌
          ├── 复用 LOGIN_CHECK 的 Tab ❌
          └── 注入实时监控钩子
              ↓
          ⚠️  Tab 被登录检测占用，钩子无法正常工作
```

**时间线**：
1. **14:03:26** - Worker 启动
2. **14:03:26** - AccountStatusReporter 创建 LOGIN_CHECK Tab，导航到 creator.douyin.com
3. **14:03:26** - MonitorTask 启动实时监控
4. **14:03:26** - 实时监控找到现有的创作者中心 Tab（LOGIN_CHECK）
5. **14:03:26** - 复用这个 Tab，注入钩子 ✅
6. **14:03:27** - 登录检测完成，可能导航走或关闭 Tab ❌
7. **结果**: 实时监控失效

---

## ✅ 解决方案

### 方案 1: 实时监控使用独立 Tab（强制创建）

**文件**: `packages/worker/src/platforms/douyin/platform.js:2966-2999`

**修改前**（复用任何创作者中心 Tab）:
```javascript
// 🔍 优先复用已存在的创作者中心 Tab（如 LOGIN_CHECK）
const accountTabs = this.browserManager.tabManager.tabs.get(account.id);
if (accountTabs) {
  for (const [tabId, tabInfo] of accountTabs.entries()) {
    try {
      const url = tabInfo.page.url();
      // ❌ 复用任何已在创作者中心的 Tab
      if (url.includes('creator.douyin.com') && !tabInfo.page.isClosed()) {
        logger.info(`♻️  复用现有 Tab ${tabId} (tag: ${tabInfo.tag}) 用于实时监控`);
        realtimePage = tabInfo.page;
        realtimeTabId = tabId;

        // 更新 Tab 标记为 REALTIME_MONITOR
        tabInfo.tag = TabTag.REALTIME_MONITOR;
        tabInfo.persistent = true;
        break;
      }
    } catch (error) {
      logger.warn(`检查 Tab ${tabId} 失败: ${error.message}`);
    }
  }
}
```

**修改后**（只复用 REALTIME_MONITOR Tag，否则强制创建新 Tab）:
```javascript
// 🔍 查找已存在的 REALTIME_MONITOR Tab（避免重复创建）
const accountTabs = this.browserManager.tabManager.tabs.get(account.id);
if (accountTabs) {
  for (const [tabId, tabInfo] of accountTabs.entries()) {
    try {
      // ✅ 只复用已标记为 REALTIME_MONITOR 的 Tab
      if (tabInfo.tag === TabTag.REALTIME_MONITOR && !tabInfo.page.isClosed()) {
        logger.info(`♻️  复用现有实时监控 Tab ${tabId}`);
        realtimePage = tabInfo.page;
        realtimeTabId = tabId;
        break;
      }
    } catch (error) {
      logger.warn(`检查 Tab ${tabId} 失败: ${error.message}`);
    }
  }
}

// 如果没找到，强制创建新的独立 Tab
if (!realtimePage) {
  logger.info(`创建独立的实时监控 Tab (账户: ${account.id})`);
  const result = await this.browserManager.tabManager.getPageForTask(
    account.id,
    {
      tag: TabTag.REALTIME_MONITOR,
      persistent: true,
      shareable: false,
      forceNew: true  // ⭐ 强制创建新 Tab，不复用其他 Tag 的 Tab
    }
  );
  realtimePage = result.page;
  realtimeTabId = result.tabId;
  logger.info(`✅ 独立实时监控 Tab 创建成功 (tabId: ${realtimeTabId})`);
}
```

**关键变更**:
- ✅ `forceNew: true` - 强制创建新 Tab，不复用其他 Tag
- ✅ 只查找 `tag === TabTag.REALTIME_MONITOR` 的 Tab
- ✅ 避免与 LOGIN_CHECK、SPIDER_DM 等其他任务冲突

---

### 方案 2: 登录检测使用默认页（PLACEHOLDER）

**文件**: `packages/worker/src/handlers/monitor-task.js:225-243`

**修改前**（使用 LOGIN_CHECK Tag）:
```javascript
// - 如果有登录任务窗口,复用它
// - 如果没有登录任务窗口,创建新的检测窗口
// - 检测后如果不是登录任务窗口,关闭它
const { TabTag } = require('../browser/tab-manager');
const { tabId, page, shouldClose } = await this.browserManager.tabManager.getPageForTask(this.account.id, {
  tag: TabTag.LOGIN_CHECK,  // ❌ 专用 Tag，会创建额外 Tab
  persistent: false,         // 检测完关闭
  shareable: true,
  forceNew: false
});

loginCheckTabId = tabId;
loginCheckPage = page;

// 导航到创作中心（如果还没在那里）
if (!page.url().includes('creator.douyin.com')) {
  logger.info('Navigating to creator center for login check...');
  await page.goto('https://creator.douyin.com/', {
    waitUntil: 'domcontentloaded',
    timeout: 30000
  });
  await page.waitForTimeout(2000);
}
```

**修改后**（使用 PLACEHOLDER Tag）:
```javascript
// ⭐ 复用默认页（PLACEHOLDER）进行登录检测，避免创建额外 Tab
const { TabTag } = require('../browser/tab-manager');
const { tabId, page, shouldClose } = await this.browserManager.tabManager.getPageForTask(this.account.id, {
  tag: TabTag.PLACEHOLDER,  // ✅ 使用默认占位页
  persistent: true,          // 保持打开
  shareable: true,           // 可共享
  forceNew: false            // 优先复用
});

loginCheckTabId = tabId;
loginCheckPage = page;

// 导航到创作中心进行登录检测
logger.info('Navigating to creator center for login check...');
await page.goto('https://creator.douyin.com/', {
  waitUntil: 'domcontentloaded',
  timeout: 30000
});
await page.waitForTimeout(2000);
```

**关键变更**:
- ✅ `tag: TabTag.PLACEHOLDER` - 使用默认页
- ✅ `persistent: true` - 检测完不关闭，供下次复用
- ✅ 不再需要检查 URL 是否在创作者中心（总是导航）

**Tab 关闭逻辑更新**:
```javascript
// ⭐ 不关闭 PLACEHOLDER Tab（持久化，供下次检测复用）
// shouldClose 为 false，因为 persistent: true
if (loginCheckTabId && shouldClose) {
  await this.browserManager.tabManager.closeTab(this.account.id, loginCheckTabId);
}
```

---

## 📊 修改前后对比

### Tab 使用情况

| 功能 | 修改前 | 修改后 | 说明 |
|------|--------|--------|------|
| 默认页 | `PLACEHOLDER` | `PLACEHOLDER` | 登录检测复用 |
| 登录检测 | `LOGIN_CHECK`（新建） | `PLACEHOLDER`（复用） | ✅ 减少 1 个 Tab |
| 实时监控 | 复用 `LOGIN_CHECK` ❌ | `REALTIME_MONITOR`（新建）| ✅ 独立 Tab |
| **总 Tab 数** | **2-3 个** | **2 个** | ✅ 更简洁 |

### Tab 冲突解决

**修改前**:
```
默认页 (PLACEHOLDER)    -→ 闲置，未使用
  ↓
创作者中心 (LOGIN_CHECK) -→ 登录检测 + 实时监控 ❌ 冲突
  ├── 登录检测占用
  └── 实时监控钩子注入但无法工作
```

**修改后**:
```
默认页 (PLACEHOLDER)           -→ 登录检测复用 ✅
  └── 检测完成后保持打开，下次继续使用

创作者中心 (REALTIME_MONITOR)  -→ 实时监控专用 ✅
  └── 独立 Tab，常驻监听
```

---

## 🧪 测试验证

### 1. 启动日志验证

**预期日志** (`douyin-platform.log`):
```
14:11:08.545 - 启动实时监控 (账户: acc-xxx)
14:11:08.545 - 查找或创建实时监控页面 (账户: acc-xxx)
14:11:08.545 - 创建独立的实时监控 Tab (账户: acc-xxx)  ← ⭐ 新日志
14:11:08.604 - ✅ 独立实时监控 Tab 创建成功 (tabId: tab-1)
14:11:12.978 - ✅ 实时监控启动成功 (账户: acc-xxx)
```

**实际日志**:
```
✅ 14:11:08.545 - 创建独立的实时监控 Tab (账户: acc-98296c87-2e42-447a-9d8b-8be008ddb6e4)
✅ 14:11:08.604 - ✅ 独立实时监控 Tab 创建成功 (tabId: tab-1)
✅ 14:11:12.978 - ✅ 实时监控启动成功
```

### 2. 浏览器 Tab 验证

**预期 Tab 数量**: 2 个
1. **默认页** (`PLACEHOLDER`) - 用于登录检测
2. **创作者中心** (`REALTIME_MONITOR`) - 实时监控专用

**验证方法**:
1. 打开浏览器，查看 Tab 栏
2. 确认有 2 个 Tab
3. 切换到创作者中心 Tab
4. 打开开发者工具 Console，执行：
   ```javascript
   typeof window.__checkRealtimeHooks
   // 预期: "function"

   window.__checkRealtimeHooks()
   // 预期: true
   ```

### 3. 功能验证

**实时监控**:
1. 在抖音创作者中心打开私信列表
2. 发送一条测试私信
3. 观察 Worker 日志，应立即捕获（0ms 延迟）

**登录检测**:
1. Worker 启动时会在默认页进行登录检测
2. 检测完成后，默认页保持打开
3. 下次检测时复用这个页面，不创建新 Tab

---

## 📁 修改文件清单

1. **`packages/worker/src/platforms/douyin/platform.js`** (2966-2999 行)
   - 修改实时监控 Tab 创建逻辑
   - 只复用 `REALTIME_MONITOR` Tag 的 Tab
   - 强制创建独立 Tab (`forceNew: true`)

2. **`packages/worker/src/handlers/monitor-task.js`** (225-243, 260-264, 278-282 行)
   - 修改登录检测使用 `PLACEHOLDER` Tag
   - 设置 `persistent: true` 保持打开
   - 更新 Tab 关闭逻辑的注释

---

## 🎯 用户体验改进

### 修改前

- ⚠️  浏览器中有 2-3 个 Tab（不确定）
- ⚠️  实时监控可能失效（被登录检测占用）
- ⚠️  登录检测创建临时 Tab 后关闭（频繁创建/销毁）

### 修改后

- ✅ 浏览器中固定 2 个 Tab（清晰明确）
- ✅ 实时监控独立运行（专用 Tab）
- ✅ 登录检测复用默认页（减少资源消耗）
- ✅ Tab 名称清晰：
  - Tab 1: "抖音精选" (PLACEHOLDER)
  - Tab 2: "抖音创作者中心" (REALTIME_MONITOR)

---

## 🔄 后续优化建议

1. **监控 Tab 数量上限**
   - 当前：每个账户最多 2 个常驻 Tab
   - 建议：添加 Tab 数量监控告警

2. **Tab 复用策略优化**
   - 当前：按 Tag 精确匹配
   - 建议：添加 Tag 优先级机制

3. **登录检测优化**
   - 当前：每次检测都导航到创作者中心
   - 建议：检测 Cookie 有效性，减少导航次数

---

**实现人**: Claude Code
**测试状态**: ✅ 已验证（日志确认）
**用户反馈**: 待确认
**部署建议**: 立即部署

---

## 附录：完整流程图

```
Worker 启动
  ↓
浏览器初始化
  ├── 创建 Context
  ├── 创建默认 Tab (PLACEHOLDER) about:blank
  └── ✅ 浏览器就绪
  ↓
登录状态检测
  ├── 获取 PLACEHOLDER Tab
  ├── 导航到 creator.douyin.com
  ├── 检测登录状态
  └── 保持 Tab 打开（persistent: true）
  ↓
MonitorTask 启动
  ├── 查找 REALTIME_MONITOR Tab
  ├── 未找到 → 创建新 Tab (forceNew: true)
  ├── 导航到 creator.douyin.com
  ├── 注入 React Fiber 钩子
  └── ✅ 实时监控激活
  ↓
定时爬虫调度
  ├── 每 5-10 分钟执行一次
  ├── 使用临时 Tab (SPIDER_DM, SPIDER_COMMENT)
  └── 任务完成后关闭临时 Tab
  ↓
系统稳定运行
  ├── 常驻 Tab 1: PLACEHOLDER (登录检测复用)
  └── 常驻 Tab 2: REALTIME_MONITOR (实时监控)
```
