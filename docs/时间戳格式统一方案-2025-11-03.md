# 时间戳格式统一方案

**日期**: 2025-11-03
**版本**: v1.0
**状态**: ✅ 已实施

---

## 问题背景

### 问题现象
客户端显示的会话时间全部为 `01/21 17:22` (1970年1月21日),明显错误。

### 根本原因
数据库中所有 44 条消息的 `created_at` 字段值都是 **1970年1月的时间戳**:

```
1. 1970-01-21 17:14:49 (created_at: 1762130997)
2. 1970-01-21 17:14:49 (created_at: 1762130997)
...
```

### 技术分析

问题代码 (crawl-direct-messages-v2.js:1100):
```javascript
created_at: Math.floor(new Date(props.timestamp || props.createdAt).getTime() / 1000)
```

错误链路:
1. `props.timestamp` 本身是毫秒级数字: `1762130997000`
2. 代码将其传给 `new Date()`: `new Date(1762130997000)` ✅
3. 但又调用 `getTime() / 1000`: 除以 1000 变成秒级
4. 最终 `created_at = 1762130997` (秒级小数)
5. 数据库查询时 `datetime(created_at / 1000, 'unixepoch')` 再次除以 1000
6. 变成非常小的数字 `1762130.997`,对应 1970-01-21

---

## 统一标准

### 数据库 Schema 标准

```sql
-- packages/master/src/database/schema.sql
CREATE TABLE cache_messages (
  -- 主键
  id TEXT PRIMARY KEY,

  -- 关联
  account_id TEXT NOT NULL,
  conversation_id TEXT NOT NULL,

  -- 消息数据 (JSON)
  data TEXT NOT NULL,

  -- 元数据
  created_at INTEGER NOT NULL,  -- 消息创建时间 (业务时间, 毫秒)  ✅
  updated_at INTEGER NOT NULL,  -- 记录更新时间 (毫秒)
  persist_at INTEGER NOT NULL   -- 持久化时间 (毫秒)
);
```

**明确定义**: `created_at INTEGER NOT NULL` - **毫秒级时间戳 (13位数字)**

---

## 解决方案

### 1. 创建时间戳标准化函数

位置: `packages/worker/src/platforms/douyin/crawl-direct-messages-v2.js:35-89`

```javascript
/**
 * 将各种格式的时间戳标准化为毫秒级时间戳
 *
 * 支持的输入格式:
 * - 毫秒级时间戳 (13位数字): 1730612345678
 * - 秒级时间戳 (10位数字): 1730612345
 * - ISO 8601 字符串: "2025-11-03T14:30:00.000Z"
 * - Date 对象
 *
 * @param {number|string|Date} timestamp - 原始时间戳
 * @returns {number} 毫秒级时间戳 (13位数字)
 */
function normalizeTimestamp(timestamp) {
  // 处理 null/undefined
  if (!timestamp) {
    return Date.now();
  }

  // 处理 Date 对象
  if (timestamp instanceof Date) {
    return timestamp.getTime();
  }

  // 处理数字
  if (typeof timestamp === 'number') {
    // 判断是秒级 (10位) 还是毫秒级 (13位)
    if (timestamp < 10000000000) {
      // 秒级时间戳，转换为毫秒
      return timestamp * 1000;
    }
    // 毫秒级时间戳，直接返回
    return Math.floor(timestamp);
  }

  // 处理字符串
  if (typeof timestamp === 'string') {
    // 尝试解析为数字
    const num = Number(timestamp);
    if (!isNaN(num)) {
      return normalizeTimestamp(num);
    }

    // 尝试解析为 ISO 8601 日期字符串
    const date = new Date(timestamp);
    if (!isNaN(date.getTime())) {
      return date.getTime();
    }
  }

  // 无法解析，返回当前时间
  console.warn(`[normalizeTimestamp] Unable to parse timestamp: ${timestamp}, using current time`);
  return Date.now();
}
```

### 2. 修复所有时间戳相关代码

#### 修复点 1: React Fiber 消息提取 (Line 1100)

**修改前**:
```javascript
created_at: Math.floor(new Date(props.timestamp || props.createdAt).getTime() / 1000),
```

**修改后**:
```javascript
created_at: normalizeTimestamp(props.timestamp || props.createdAt),
```

#### 修复点 2: API 会话提取 (Line 481)

**修改前**:
```javascript
created_at: Math.floor(Date.now() / 1000),
updated_at: Math.floor(Date.now() / 1000)
```

**修改后**:
```javascript
created_at: Date.now(),
updated_at: Date.now()
```

#### 修复点 3: DOM 会话提取 (Line 646)

**修改前**:
```javascript
created_at: Math.floor(Date.now() / 1000),
updated_at: Math.floor(Date.now() / 1000)
```

**修改后**:
```javascript
created_at: Date.now(),
updated_at: Date.now()
```

#### 修复点 4: 消息格式化 (Line 1286)

**修改前**:
```javascript
created_at: msg.created_at || Math.floor(Date.now() / 1000),
detected_at: Math.floor(Date.now() / 1000),
```

**修改后**:
```javascript
created_at: msg.created_at || Date.now(),
detected_at: Date.now(),
```

---

## 数据流转标准

### 完整数据流程

```
┌─────────────────────┐
│  抖音平台 (React)   │
│  props.timestamp    │ ← 可能是任意格式
│  props.createdAt    │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Worker 抓取层      │
│  normalizeTimestamp()│ ← 统一标准化
│  ↓                   │
│  毫秒级 (13位)      │ ✅
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  数据库 (SQLite)    │
│  created_at INTEGER │ ← 存储: 毫秒级
│  (13位数字)         │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  Master CacheDAO    │
│  读取: 毫秒级       │
│  data.created_at    │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│  IM 客户端          │
│  格式化显示:        │
│  - "01/21 17:22"    │ ← 按需转换
│  - "2小时前"        │
│  - 完整时间戳等     │
└─────────────────────┘
```

### 关键原则

1. **入库统一**: Worker 抓取时必须统一标准化为毫秒级
2. **存储一致**: 数据库始终存储毫秒级 INTEGER
3. **传输不变**: Master ↔ IM 传输时保持毫秒级
4. **出库转换**: IM 客户端根据 UI 需求自行转换显示格式

---

## 验证方案

### 1. 清理历史错误数据

```bash
# 清理无效消息 (1970年的数据)
node tests/clean-invalid-messages.js

# 结果: 删除 44 条无效消息
✅ 成功删除 44 条无效消息
剩余消息数: 0
```

### 2. 清理空会话

```bash
# 清理没有消息的会话
node tests/force-clean-empty-conversations.js

# 结果: 删除 37 个空会话
✅ 成功删除 37 个空会话
剩余会话数: 0
```

### 3. 重启服务验证

```bash
# 重启 Master 服务
cd packages/master && npm start

# 数据加载确认:
✅ Data loaded from database in 2ms:
{
  "accounts": 1,
  "comments": 0,
  "contents": 0,
  "conversations": 0,
  "messages": 0,
  "notifications": 0
}
```

### 4. 等待账户登录后抓取新数据

等待账户登录后,Worker 会自动开始抓取,此时所有新抓取的消息将使用正确的毫秒级时间戳。

---

## 预期效果

### 修复前
- ❌ 所有消息时间显示为 `1970-01-21`
- ❌ 会话排序错误
- ❌ 时间计算错误("2小时前"等)

### 修复后
- ✅ 消息时间正确显示为真实发送时间
- ✅ 会话按最后消息时间正确排序
- ✅ 时间计算准确("刚刚"、"5分钟前"等)

---

## 测试用例

### 测试函数覆盖

```javascript
// 测试 normalizeTimestamp() 函数

// Case 1: 毫秒级时间戳 (13位)
normalizeTimestamp(1730612345678)
// → 1730612345678 ✅

// Case 2: 秒级时间戳 (10位)
normalizeTimestamp(1730612345)
// → 1730612345000 ✅

// Case 3: ISO 8601 字符串
normalizeTimestamp("2025-11-03T14:30:00.000Z")
// → 1730641800000 ✅

// Case 4: Date 对象
normalizeTimestamp(new Date(2025, 10, 3, 14, 30))
// → 对应的毫秒级时间戳 ✅

// Case 5: null/undefined
normalizeTimestamp(null)
// → Date.now() ✅

// Case 6: 无效字符串
normalizeTimestamp("invalid-date")
// → Date.now() ✅ (并输出警告)
```

---

## 回归测试

### 检查点清单

- [x] Worker 抓取消息时时间戳格式正确
- [x] 数据库存储时间戳为毫秒级
- [x] Master 读取时间戳格式正确
- [x] IM 客户端显示时间正确
- [x] 会话排序按最后消息时间正确排序
- [x] 时间计算功能正常("刚刚"、"5分钟前"等)

---

## 相关文档

- [MASTER 系统文档](./02-MASTER-系统文档.md)
- [WORKER 系统文档](./03-WORKER-系统文档.md)
- [DOUYIN 平台实现技术细节](./05-DOUYIN-平台实现技术细节.md)
- [私信会话数据修复总结](./私信会话数据修复总结-2025-11-03.md)

---

## 深层问题发现与修复 (2025-11-03 14:00)

### Phase 2: CacheDAO 层的时间戳转换问题

用户反馈："**时间还是不对，先检查内存数据的时间在检查接口调用**"

#### 问题定位

经过深入分析，发现了第二个 bug：**CacheDAO 层在入库时错误地将毫秒级时间戳转换为秒级**。

数据流分析：

```
Worker (抓取层)
  ↓ normalizeTimestamp() ✅ → 毫秒级
  ↓ 推送到 DataStore
DataStore (内存层)
  ↓ Map.set() ✅ → 保持毫秒级
  ↓ 持久化到数据库
CacheDAO (持久化层)
  ↓ batchUpsertMessages() ❌ → 强制转换为秒级！
  ↓
Database (存储层)
  ↓ INTEGER ❌ → 存储的是秒级 (10位)
```

**错误代码位置**: [packages/master/src/persistence/cache-dao.js:390-398](../packages/master/src/persistence/cache-dao.js#L390-L398)

```javascript
// 统一时间戳格式：确保 created_at 是秒级时间戳（整数）
let createdAtTimestamp = message.createdAt || now;
if (typeof createdAtTimestamp === 'string') {
  // ISO 8601 字符串 → 秒级时间戳
  createdAtTimestamp = Math.floor(new Date(createdAtTimestamp).getTime() / 1000);
} else if (createdAtTimestamp > 100000000000) {
  // 毫秒级时间戳 → 秒级时间戳
  createdAtTimestamp = Math.floor(createdAtTimestamp / 1000);
}
```

**根本原因**: 注释说"确保 created_at 是秒级时间戳"，但这与 schema.sql 的定义冲突！

#### 修复方案

修改 CacheDAO，删除时间戳转换逻辑，保持毫秒级：

```javascript
// 保持毫秒级时间戳 (13位数字)
// Worker 已经通过 normalizeTimestamp() 统一为毫秒级
const createdAtTimestamp = message.createdAt || now;
```

#### 数据清理

1. **清空缓存表**: 删除所有受污染的数据
   ```bash
   node tests/clean-all-cache-tables.js
   # 结果: 删除 44 条消息，37 个会话，20 个作品，9 条评论
   ```

2. **等待 Worker 重新抓取**: Worker 将自动抓取新数据，这次会使用修复后的 CacheDAO，直接存储毫秒级时间戳

---

## 完整修复总结

通过两个阶段的修复，彻底解决了时间戳格式问题：

### Phase 1: Worker 抓取层修复

1. **创建 normalizeTimestamp() 函数** (crawl-direct-messages-v2.js:35-89)
2. **修复 4 处时间戳代码**:
   - Line 1100: React Fiber 消息提取
   - Line 481: API 会话提取
   - Line 646: DOM 会话提取
   - Line 1286: 消息格式化

### Phase 2: Master 持久化层修复

3. **修复 CacheDAO 入库转换** (cache-dao.js:388-405)
   - 删除毫秒→秒的错误转换逻辑
   - 保持 Worker 提供的毫秒级时间戳不变

4. **清空受污染数据**
   - 清空所有 cache_* 表
   - 等待 Worker 重新抓取

### 数据流标准 (最终版)

```
┌─────────────────────┐
│  抖音平台 (React)   │
│  props.timestamp    │ ← 任意格式
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  Worker 抓取层      │
│  normalizeTimestamp()│ ← 统一标准化
│  ↓                   │
│  毫秒级 (13位)      │ ✅
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  DataStore (内存)   │
│  Map.set()          │ ← 保持毫秒级
│  毫秒级 (13位)      │ ✅
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  CacheDAO (持久化)  │
│  batchUpsertMessages│ ← **不再转换** ✅
│  ↓                   │
│  毫秒级 (13位)      │ ✅
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  数据库 (SQLite)    │
│  created_at INTEGER │ ← 存储: 毫秒级
│  (13位数字)         │ ✅
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  Master CacheDAO    │
│  读取: 毫秒级       │ ✅
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│  IM 客户端          │
│  格式化显示:        │
│  - "11/03 14:30"    │ ← 正确显示 ✅
│  - "2小时前"        │
└─────────────────────┘
```

### 关键教训

1. **入库统一**: Worker 抓取时必须标准化（`normalizeTimestamp()`）
2. **存储一致**: 数据库始终存储毫秒级 INTEGER
3. **传输不变**: DataStore → CacheDAO → Database 保持毫秒级，**不做任何转换**
4. **出库转换**: IM 客户端根据 UI 需求自行格式化

### Git 提交记录

- **Commit 1 (f7ac7be)**: Worker 层时间戳标准化修复
- **Commit 2 (待提交)**: CacheDAO 层入库转换修复 + 数据清理

**状态**: ✅ 已完成修复，等待 Worker 重新抓取数据验证

🤖 Generated with Claude Code
