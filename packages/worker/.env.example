# Worker进程配置

# Worker ID (自动生成或手动指定)
WORKER_ID=worker-001

# 主控服务地址
MASTER_HOST=localhost
MASTER_PORT=3000

# Worker端口
WORKER_PORT=4000

# 数据库路径
DB_PATH=./data/worker.db

# 日志级别
LOG_LEVEL=info

# 环境
NODE_ENV=development

# ========================================
# 浏览器配置
# ========================================
# 系统使用多Browser架构:
#   - 每个账户独立Browser进程
#   - 100%指纹隔离,无关联风险
#   - 指纹稳定持久化
#   - 内存: ~200MB/账户
#   - 建议最大账户数: ≤10个/Worker
#
# 详细文档: ../../.docs/architecture/多Browser架构详解.md

# 浏览器是否无头模式 (false=显示浏览器窗口,方便调试)
HEADLESS=true
