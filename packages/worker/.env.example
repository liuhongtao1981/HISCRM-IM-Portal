# Worker进程配置

# Worker ID (自动生成或手动指定)
WORKER_ID=worker-001

# 主控服务地址
MASTER_HOST=localhost
MASTER_PORT=3000

# Worker端口
WORKER_PORT=4000

# 数据库路径
DB_PATH=./data/worker.db

# 日志级别
LOG_LEVEL=info

# 环境
NODE_ENV=development
