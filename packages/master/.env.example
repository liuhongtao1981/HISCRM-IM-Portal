# 主控服务配置

# 服务端口
PORT=3000

# 数据库路径
DB_PATH=./data/master.db

# 加密密钥 (用于账户凭证加密, 请使用强随机密钥)
ENCRYPTION_KEY=your-32-character-encryption-key

# 日志级别 (debug, info, warn, error)
LOG_LEVEL=info

# Worker配置
WORKER_MAX_COUNT=10
WORKER_HEARTBEAT_TIMEOUT=30000

# 环境
NODE_ENV=development
